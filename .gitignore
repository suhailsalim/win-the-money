# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# OpenWolf — local project-intelligence + token ledger (regenerate with `openwolf init`)
.wolf/

# Claude Code — local hooks/settings stay ignored (OpenWolf re-registers these on
# `openwolf init`), but project skills are shared and committed.
.claude/*
!.claude/skills/

# Local tooling / scratch
/tmp/
*.log
# Real bank/card statements must never be committed (AGENTS.md). Ignore all PDFs.
*.pdf
site/

# Parser-harness fixtures: real statements AND the passwords that open them. `*.pdf` above already
# covers the statements, but passwords.json is plain text — ignore the whole directory so a filled-in
# password file can never be staged by accident. Local-only; see plans/PLAN-parser-regression-harness.md.
tools/parser-harness/fixtures/
tools/parser-harness/a.out
tools/parser-harness/*.o
